/**
 * @file payments-config.model.js
 * @description Data model and default labels for PaymentsConfig admin component.
 */

export const PaymentsConfigDefaults = {
  TITLE: 'Direct UPI payments',
  SUBTITLE: 'UPI fallback',
  DESCRIPTION: 'Shown to guests as a backup when Razorpay doesn\'t work. Leave both fields empty to hide the option. Guests who pay this way appear as "UPI claimed" in the booking desk — verify in your UPI app, then Mark paid.',
}
